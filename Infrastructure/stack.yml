version: "3.1"

services:
  puts:
    image: puts
    restart: always
    ports:
      - 8000:80
    volumes:
      - keys:/app/Keys
    depends_on:
      - db
    environment:
      ConnectionStrings__ProblemDatabase: server=db;user=root;password=dbadmin;database=puts
      ASPNETCORE_URLS: http://+:80
  db:
    image: mariadb
    restart: always
    ports:
      - 8002:3306
    volumes:
      - db-data:/var/lib/mysql
    environment:
      MARIADB_ROOT_PASSWORD: dbadmin

  adminer:
    image: adminer
    restart: always
    ports:
      - 8003:8080

volumes:
  db-data:
  keys:
